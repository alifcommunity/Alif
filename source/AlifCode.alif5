دالة سرعة(المسافة, الزمن = 1):
    ارجع المسافة \ الزمن

سرعة_سقوط_الكرة = سرعة(10, 4)

اطبع(سرعة_سقوط_الكرة)

دالة تجربة(ب, س = 7,):
	اطبع(ب)
	اطبع(س)

دالة هل_اولي(عدد):
	اذا عدد == 2:
		ارجع عدد
	اذا عدد <= 1 او ليس عدد \\ 2:
		ارجع
	لاجل مقسوم في (3,عدد):
		اذا ليس عدد \\ مقسوم:
			ارجع
	ارجع عدد

دالة تشغيل_البرنامج(عدد):
	لاجل ب في (عدد):
		اطبع(هل_اولي(ب))


س = ادخل()
اطبع(س)

تجربة(9)

تشغيل_البرنامج(10)

